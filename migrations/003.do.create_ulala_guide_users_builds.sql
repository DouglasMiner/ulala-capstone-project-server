CREATE TABLE ulala_guide_users_builds (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id INTEGER REFERENCES ulala_guide_users(id) ON DELETE CASCADE NOT NULL,
  build_id INTEGER REFERENCES ulala_guide_builds(id) ON DELETE CASCADE NOT NULL
);